version: "3"

tasks:
  sqlc:
    cmds:
      - sqlc generate
  frontend-dev:
    cmds:
      - npm run openapi-ts --prefix ./frontend
      - npm run dev --prefix ./frontend
    internal: true
  backend-dev:
    cmd: go run .
    internal: true
  dev:
    deps: [backend-dev, frontend-dev]
  
